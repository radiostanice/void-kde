# Theme for the void-kde installer.
use_colors = ON
use_shadow = OFF

# Body and borders
screen_color = (WHITE,BLACK,OFF)
dialog_color = (WHITE,BLACK,OFF)
shadow_color = (BLACK,BLACK,OFF)
border_color = (CYAN,BLACK,ON)
border2_color = (CYAN,BLACK,ON)
title_color = (CYAN,BLACK,ON)

# Buttons (< OK > etc.)
button_active_color = (BLACK,GREEN,ON)
button_inactive_color = (WHITE,BLACK,ON)
button_key_active_color = (BLACK,GREEN,ON)
button_key_inactive_color = (WHITE,BLACK,ON)
button_label_active_color = (BLACK,GREEN,ON)
button_label_inactive_color = (WHITE,BLACK,ON)

# Menu area (Fixes the grey "sr0 1G" text)
menubox_color = (WHITE,BLACK,OFF)
menubox_border_color = (CYAN,BLACK,ON)
menubox_border2_color = (CYAN,BLACK,ON)
item_color = (WHITE,BLACK,ON)
tag_color = (WHITE,BLACK,ON)
tag_key_color = (WHITE,BLACK,ON)

# Selected row (Fixes the white first letter on green)
item_selected_color = (BLACK,GREEN,ON)
tag_selected_color = (BLACK,GREEN,ON)
tag_key_selected_color = (BLACK,GREEN,ON)

# Other elements
check_color = (WHITE,BLACK,ON)
check_selected_color = (BLACK,GREEN,ON)
inputbox_color = (WHITE,BLACK,ON)
inputbox_border_color = (CYAN,BLACK,ON)
inputbox_border2_color = (CYAN,BLACK,ON)
position_indicator_color = (CYAN,BLACK,ON)
uarrow_color = (CYAN,BLACK,ON)
darrow_color = (CYAN,BLACK,ON)
gauge_color = (BLACK,CYAN,ON)
itemhelp_color = (WHITE,BLACK,OFF)
